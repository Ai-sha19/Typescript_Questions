// Determine whether the number is odd or even.
var number = 7;
var result = number % 2 === 1 ? "The number is odd." : "The number is even.";
console.log(result);
// Check if the weather is hot or cold based on temperature.
var temperature = 14;
var weather = temperature > 25 ? "The weather is hot." : "The weather is cold.";
console.log(weather);
