// Determine whether the number is odd or even.

let number: number = 7;

let result = number % 2 === 1 ? "The number is odd." : "The number is even.";

console.log(result);


// Check if the weather is hot or cold based on temperature.

let temperature: number = 14;

let weather = temperature > 25 ? "The weather is hot." : "The weather is cold.";

console.log(weather);





